# RLAIF RAG Agent — pinned production dependencies
# Install with: pip install -r requirements.txt

# LLM & LangChain
openai>=1.30.0,<2.0.0
langchain>=0.2.0,<0.4.0
langchain-core>=0.2.0,<0.4.0
langchain-community>=0.2.0,<0.4.0
langchain-openai>=0.1.0,<0.4.0
faiss-cpu>=1.8.0,<2.0.0

# PDF loading
pypdf>=4.0.0,<5.0.0

# Config / validation
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# Resilience
tenacity>=8.2.0,<10.0.0

# Service
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
slowapi>=0.1.9,<1.0.0          # rate limiting for FastAPI

# Dev / test (install separately with: pip install -r requirements-dev.txt)
# Listed here for reference — move to requirements-dev.txt if preferred.
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
httpx>=0.27.0,<1.0.0           # AsyncClient for FastAPI tests
ruff>=0.4.0,<1.0.0
mypy>=1.10.0,<2.0.0
